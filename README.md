# OpenList-Resource

The OpenList-Resource is a repository for storing the resources that are used in the OpenList project.

This is a fork of the original OpenList repository, optimized for faster access speeds.

## Features

This repository contains all the content of the original repository, with additional features added.
- 3D model viewing with 3DViewer